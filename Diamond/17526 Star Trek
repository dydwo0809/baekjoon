#include <bits/stdc++.h>
using namespace std;

typedef long long ll;


struct line {  
   mutable ll m, k, p;  
   bool operator<(const line &o) const { return m < o.m; }  
   bool operator<(ll x) const { return p < x; }  
};

struct LineContainer : multiset<line, less<>> {  
   //(for doubles, use inf = 1/.0, div(a,b) = a/b)  
   const static ll inf = LLONG_MAX;  

   ll div(ll a, ll b) { 
      return a / b - ((a ^ b) < 0 && a % b); 
   } 

   bool isect(iterator x, iterator y) {  
      if (y == end()) return x->p = inf, 0;  
      if (x->m == y->m) x->p = x->k > y->k ? inf : -inf;  
      else x->p = div(y->k - x->k, x->m - y->m);  
      return x->p >= y->p;  
   }  

   void add(ll m, ll k) { //add y = mx + k  
      auto z = insert({m, k}), y = z++, x = y;  
      while (isect(y, z)) z = erase(z);  
      if (x != begin() && isect(--x, y)) isect(x, y = erase(y));  
      while ((y = x) != begin() && (--x)->p >= y->p) isect(x, erase(y));  
   }  

   ll query(ll x) {  
      auto l = *lower_bound(x);  
      return l.m * x + l.k;  
   }  
} cht;

int main() {
   ios_base::sync_with_stdio(false);
   cin.tie(0);
   cout.tie(0);

   int n;
   cin >> n;
   vector<ll> s(n);
   int i;
   ll temp = 0;
   ll num;
   s[0] = temp;
   for (i = 1; i < n; i++){
      cin >> num;
      temp += num;
      s[i] = temp;
   }

   vector<ll> pt(n - 1);
   vector<ll> sp(n - 1);

   for (i = 0; i < n - 1; i++){
      cin >> pt[i];
      cin >> sp[i];
   }

   vector<ll> dp(n);
   dp[0] = 0;
   cht.add(-sp[0], -dp[0] - pt[0] + sp[0] * s[0]);
   for (i = 1; i < n; i++){
      dp[i] = -cht.query(s[i]);
      cht.add(-sp[i], -dp[i] - pt[i] + sp[i] * s[i]);
   }

   cout << dp[n - 1];

   return 0;
}
