n = int(input())

nums = [i for i in range(1, n + 1)]

for i in range(n - 1):
    temp = i
    while temp > 0:
        next = (temp - 1) // 2
        nums[temp], nums[next] = nums[next], nums[temp]
        temp = next
    nums[0], nums[i + 1] = nums[i + 1], nums[0]

print(*nums)
